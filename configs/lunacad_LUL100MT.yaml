_BASE_: Base-COCO-InstanceSegmentation.yaml
OUTPUT_DIR: "./output"
MODEL:
  BACKBONE:
    FREEZE_AT: 0
    NAME: "build_resnet_backbone"
  WEIGHTS: "detectron2://ImageNetPretrained/torchvision/R-50.pkl"
  PIXEL_MEAN: [128.0, 128.0, 128.0]
  PIXEL_STD: [58.0, 58.0, 58.0]
  RESNETS:
    DEPTH: 50
    OUT_FEATURES: ["res2", "res3", "res4", "res5"]
  META_ARCHITECTURE: "LunaCAD"
  SEM_SEG_HEAD:
    NAME: "Model_Head"
    IGNORE_VALUE: 255
    NUM_CLASSES: 1
    LOSS_WEIGHT: 1.0
    CONVS_DIM: 256
    MASK_DIM: 256
    NORM: "GN"
    # pixel decoder
    PIXEL_DECODER_NAME: "ETCA_Encoder"  
    DIM_FEEDFORWARD: 1024
    NUM_FEATURE_LEVELS: 3
    TOTAL_NUM_FEATURE_LEVELS: 4
    IN_FEATURES: ["res2", "res3", "res4", "res5"]
    DEFORMABLE_TRANSFORMER_ENCODER_IN_FEATURES: ["res2", "res3", "res4", "res5"]
    COMMON_STRIDE: 4
    TRANSFORMER_ENC_LAYERS: 3
    SEM_THRESHOLD: 0.1
    PIXEL_THRESHOLD: 0.5
    DEFORMABLE_TRANSFORMER_ENCODER_N_HEADS: 8
    DEFORMABLE_TRANSFORMER_ENCODER_N_POINTS: 4
    FEATURE_ORDER: 'high2low'  # ['low2high', 'high2low'] high2low: from high level to low level
  DECODER:
    TRANSFORMER_DECODER_NAME: "Task3_Decoder"
    DEEP_SUPERVISION: True
    NO_OBJECT_WEIGHT: 0.1
    CLASS_WEIGHT: 4.0
    MASK_WEIGHT: 5.0
    DICE_WEIGHT: 5.0
    BOX_LOSS: True
    BOX_WEIGHT: 5.0
    GIOU_WEIGHT: 2.0
    COST_CLASS_WEIGHT: 4.0
    COST_MASK_WEIGHT: 5.0
    COST_DICE_WEIGHT: 5.0
    COST_BOX_WEIGHT: 5.0
    COST_GIOU_WEIGHT: 2.0
    # # if you use different feature dimension (d_model), remember to modify size of position embedding to half of d_model (moba_net/utils/utils.py, Line 82)
    # 256 -> 128, 64 -> 32
    HIDDEN_DIM: 256
    NUM_OBJECT_QUERIES: 1200
    NHEADS: 8
    DROPOUT: 0.0
    DIM_FEEDFORWARD: 2048
    ENC_LAYERS: 0
    PRE_NORM: False
    ENFORCE_INPUT_PROJ: False
    SIZE_DIVISIBILITY: 1
    DEC_LAYERS: 1
    TRAIN_NUM_POINTS: 10000
    OVERSAMPLE_RATIO: 3.0
    IMPORTANCE_SAMPLE_RATIO: 0.75
    INITIAL_PRED: True
    INITIALIZE_BOX_TYPE: "bitmask"
    SEMANTIC_CE_LOSS: False # Only set it to True if you are training a pure semantic segmentation model (like DeepLab, Mask2Former-SemanticOnly).
    TEST:
      SEMANTIC_ON: True
      INSTANCE_ON: True
      PANOPTIC_ON: False
      OVERLAP_THRESHOLD: 0.8
      OBJECT_MASK_THRESHOLD: 0.1
      PANO_TEMPERATURE: 0.06
      PANO_TRANSFORM_EVAL: False
      # NMS post-processing: filter duplicate boxes/masks around the same target
      NMS_ENABLED: False
      NMS_TYPE: "segm"       # "bbox" (box IoU) | "segm" (mask IoU)
      NMS_THRESH: 0.5
  MASK_ON: True
  ANCHOR_GENERATOR:
    SIZES: [[8, 16, 32, 64, 128]]        # default: 32-512
  MOE:
    ATTN_TYPE_ENC: "etca"
    FFN_TYPE_ENC: "moffn"
    NUM_EXPERTS: 4
    K: 2
    N_POINTS: 4
    NOISY_GATING: True
    W_TOPK_LOSS: 0.01
    W_SWITCH_LOSS: 0.01
    W_Z_LOSS: 0.01
    ACC_AUX_LOSS: True
    W_SAMPLING_RANK: 0.01
  UAGR:
    ENABLED: True
    NUM_BALLS: 16
    USE_DIAG_COV: True
    TAU: 1.0
    POSITION: "post_ffn"    # "post_attn" | "post_ffn" | "pre_attn" | "parallel"
    W_DIV: 0.01
    W_SCALE: 0.01
  HGC:
    ENABLED: True
    TAU: 1.0
    WEIGHT: 0.01
DATASETS:
  TRAIN: ("all2017-2",)
  TEST: ("val2017",)
SOLVER:
  IMS_PER_BATCH: 1
  BASE_LR: 0.0001
  ACCUMULATION_STEPS: 1
  STEPS: (18000, 19000)

  # LUL100MT
  MAX_ITER: 100100
  LR_DROP_ITERS: [96000, 97000, 98000, 99000, 100000]
  LR_MULTIPLIER: [1.0, 0.5, 0.25, 0.125, 0.0625, 0.03125]
  WARMUP_ITERS: 1000
  AREA_RANGE_SETTING: [100, 500] # 10km 50km
  # 仅 LUL100MT: 尺度划分改用检测框对角线长度 sqrt(w^2+h^2) (像素),
  # <100 为 small, 100~500 为 medium, >500 为 large; 其他数据集保持 area 划分
  DIAGONAL_RANGE_SETTING: [100, 500]
    
  WARMUP_FACTOR: 1.0
  WEIGHT_DECAY: 0.05
  OPTIMIZER: "ADAMW"
  BACKBONE_MULTIPLIER: 0.1
  CLIP_GRADIENTS:
    ENABLED: True
    CLIP_TYPE: "full_model"
    CLIP_VALUE: 0.01
    NORM_TYPE: 2.0
  AMP:
    ENABLED: True
  CHECKPOINT_PERIOD: 1000000
  PRINT_PERIOD: 20  # modify miniconda3/lib/python3.12/site-packages/detectron2/engine/defaults.py Line 499 period=cfg.SOLVER.PRINT_PERIOD
INPUT:
  # LUL100MT 896
  IMAGE_SIZE: 896
  MIN_SCALE: 1.0
  MAX_SCALE: 1.0
  MIN_SIZE_TRAIN: (896,)
  MAX_SIZE_TRAIN: 896
  MIN_SIZE_TEST: 896
  MAX_SIZE_TEST: 896
  FORMAT: "RGB"
  DATASET_MAPPER_NAME: "coco_instance_lsj"
  SIZE_DIVISIBILITY: 1
TEST:
  EVAL_PERIOD: 500
  # [train]  ChangE 217
  # [val]    ChangE 217
  DETECTIONS_PER_IMAGE: 24
  MAX_NUM_TARGETS: 24
  BEST_METRIC: -1.
  MAIN_TASK: "segm"
  MAIN_METRIC: "AP50"
#  EVAL_FLAG: 1




